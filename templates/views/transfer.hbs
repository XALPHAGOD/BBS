<!DOCTYPE html>
<html lang="en">
<head>
    {{>links}}
    <title>Transfer</title>
</head>
<body class="d-flex justify-content-center align-items-center min-vh-100">
    <div class="container mt-5 d-flex flex-column justify-content-center align-items-center">
        
        <form action="/transfer" method="POST" class="p-3 w-50" style="background-color: #F7F7F7; box-shadow: 0 1px 10px #CDCDCD;">
            <div class="row mb-3">
                <h3 class="text-center">Balance: {{user.balance}}</h3>
            </div>

            <div class="row mb-3">
                <label for="from" class="form-label col-12 col-sm-2">From</label>
                <select class="form-select col-12 col-sm offset-sm-2" name="from" id="from">
                    <option value={{user._id}} selected>{{user.username}}</option>
                </select>
            </div>

            <div class="row mb-3">
                <label for="to" class="form-label col-12 col-sm-2">To</label>
                <select class="form-select col-12 col-sm offset-sm-2" name="to" id="to">
                    {{#each allUsers}}
                        <option value={{this._id}}>{{this.username}}</option>
                    {{/each}}
                </select>
            </div>

            <div class="row mb-3">
                <label for="amount" class="form-label col-12 col-sm-2">Amount</label>
                <input type="number" class="form-control col-12 col-sm offset-sm-2" name="amount" id="amount" onkeydown="return (event.keyCode!=109);">
            </div>

            <div class="row mb-3">
                <button type="submit" class="btn btn-primary col-6 col-sm-4 offset-sm-4">Transfer</button>
            </div>
            
        </form>

        <div class="row mb-5 w-50 d-flex justify-content-around">
            <a class="btn btn-primary text-white col-sm-4 my-3" href="/customers/{{user._id}}">Back</a>
            <a class="btn btn-primary text-white col-sm-4 my-3" href="/home">Home</a>
        </div>

    </div>
    {{>closing}}
</body>
</html>