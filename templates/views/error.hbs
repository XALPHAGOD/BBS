<!DOCTYPE html>
<html lang="en">
<head>
    {{>links}}
    <title>Error</title>
</head>
<body class="d-flex flex-column justify-content-center min-vh-100">
    {{>header}}
    <div class="container d-flex flex-column align-items-center"> 
        <h1 class="text-center text-danger">Error</h1>  
        <h3 class="text-center">{{err}}</h3>
        <h3 class="text-center">{{message}}</h3>
        <a class="btn btn-primary mt-5" href="/home">Okay</a>
    </div>
    {{>footer}}
    {{>closing}}
</body>
</html>